import 'package:flutter/material.dart';

class WaldekAIScreen extends StatelessWidget {
  const WaldekAIScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Waldek AI Chatbot'),
      ),
    );
  }
}
